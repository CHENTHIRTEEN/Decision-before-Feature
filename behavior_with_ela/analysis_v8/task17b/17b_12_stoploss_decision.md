Task17B 是零新增目标函数评估的机制分解。所有正式区间均按 `cv_group_id` 分组 bootstrap 5000 次；`new_objective_FE = 0`。

# 17b_12 Stop-loss decision

最终判定：**D2 NOISE-LIMITED DECISION GEOMETRY**。

实验主线停止；不得以新增特征、selector、repetition、seeds 6-10 或 CEC 扩充证据。D2 只允许评估现有结果是否足以收束为机制稿，不自动给出 paper-consolidation GO。
