# 2026-08-11 项目整理记录

本次整理以“正式实验主线清晰、历史材料可恢复”为目标。旧实验结果、模型与生成文件均已退出活动路径；批量永久删除约 9.2 GB 数据被安全策略拒绝，因此采用项目内可恢复归档。`results/` 下当前除 `archive/` 外没有实验产物，后续正式结果必须按三档 query 协议重新生成。

## 当前正式结果状态

- 没有可作为论文证据的现成 trajectory、query feature、Selection Reference、utility label、Decision 模型或外部评价结果。
- 活动数据目录将在重生成时按 `README.md` 与三档 Landscape Query 协议创建。
- 不得从 `results/archive/` 恢复模型后继续训练，也不得把归档数值写入论文主表。

## 归档位置

- `results/archive/retired_preformal/`：旧 phase1、MVE、preliminary/min_support 数据与诊断结果。
- `results/archive/cec2017_preliminary/`：旧 Ridge controller 的 CEC2017 预运行与决策检查频率诊断。
- `results/archive/regenerable/`：Random Forest 大模型、重复 threshold-sweep CSV、Python cache 和生成元数据。
- `results/archive/withdrawn_20260811/`：原活动路径中的旧 BBOB trajectory、16 维旧 landscape 表、静态 Selection Reference、utility labels、Decision 模型、预测、图表和报告。
- `docs/archive/min_support/`：preliminary/min_support 的关键归因矩阵与计划文档。
- `results/archive/workfiles/`：WPS 中间图和未采用的生成图。

## 从当前运行面移除

以下已跟踪内容已删除，但可由 Git 历史恢复：

- preliminary/min_support 专用 Decision 源码；
- preliminary/min_support 与 MVE 配置；
- 单问题旧 phase1 collector 入口；
- 外置 `full_training_readiness` 启动许可脚本与命令；必要的数据、输入字段、标签和 family split 检查改由各训练入口直接执行。既有同名结果仅为已完成正式运行保留原始上下文，不再作为后续运行条件；
- 已完成的临时任务说明文档。
- 旧 `results/decision/` 中被 Git 跟踪的模型比较与调参报告；归档副本仅存在于本机忽略目录，不随 GitHub 发布。

## 恢复说明

归档结果没有修改内容，只改变路径。若确需复查历史运行，应先明确具体归档路径和研究目的；不得把归档数据重新混入正式 phase1 训练、模型选择或论文主表。GitHub 仅同步代码、协议和撤回说明，不同步本机 `results/archive/`。
