# Image-generation prompt

Use case: infographic-diagram

Asset type: wide landscape scientific workflow architecture figure for an Elsevier engineering journal manuscript.

Create a clean, publication-ready workflow figure titled exactly “Decision-before-Feature: Deciding Whether Landscape Analysis Is Worth Performing in Black-Box Optimization”. Use a wide 16:9 white canvas, flat 2D vector-like styling, thin consistent dark-gray strokes, muted light-blue, light-teal, light-gray, and pale-orange fills, clear horizontal sans-serif typography, aligned boxes, balanced spacing, and only PowerPoint/draw.io-reproducible shapes. Use solid arrows for data flow and one subtle dashed arrow for evaluation feedback. Do not use gradients, 3D landscapes, surface plots, neural-network diagrams, photos, decorative backgrounds, complex icons, shadows, perspective, logos, or watermarks.

Arrange three horizontal bands. In “Offline data generation”, show “Black-box benchmark problems” with “BBOB train / validation; CEC and engineering tests”, then “Portfolio optimizer trajectories” with “DE, PSO, CMA-ES, SHADE”, then “Cheap optimization probing” with “Early FE-indexed search history”. Connect them left to right.

In “Decision-before-Feature model”, connect probing downward into “Algorithm-independent behavior features” with “improvement rate, diversity, entropy, stagnation, distance decay”, then “Search maturity state” with “Is the search state informative enough?”, then “Decision Model” with “Query landscape descriptors or skip?”. Emphasize “When is landscape analysis worth its cost?” in a pale-orange callout. Create two mutually exclusive branches with no connection between them: “Skip landscape analysis”; and “Perform landscape analysis” leading to “Landscape descriptors”, with “descriptor features acquired with FE cost” and “query time: deployment / resource evaluation only”.

In “Utility and evaluation”, make both branches converge into “Selection Reference” with “continue current or switch to another optimizer”. Continue to “Five paired policy paths”, listing exactly: “skip”, “query_joint”, “query_matched_state_only”, “sampling_only_continue_current”, and “behavior_only_full_budget”. Continue to “ELA Utility” with “FE-indexed terminal loss improvement” and “query time reported separately”, then “Policy evaluation” with “run-level first trigger; function-family split; baselines”.

Add a right-side “Baselines” panel listing exactly: “Never ELA”, “Always ELA”, “Random Analysis”, “Traditional AAS”, “SBS / VBS”, and “Time-only Controller”. Add a dashed boundary labeled “Offline supervised learning” and a separate dashed boundary labeled “predefined validation / testing”. Add a bottom note box with the exact sentence: “Decision inputs exclude optimizer parameters, algorithm ID, function ID, known optimum gap, and ELA descriptors before querying.” Preserve all supplied labels verbatim and keep the main scientific utility FE-indexed; query time is reported separately for deployment/resource evaluation and is not part of the scientific label.

Right-side evaluation layout: render “Baselines” as a compact seven-row table fully enclosed by the “predefined validation / testing” dashed boundary and vertically aligned with “Policy evaluation”. Draw a solid arrow from the left edge of the Baselines table pointing left into Policy evaluation, indicating that baseline results are comparison inputs. Draw exactly one separate dashed feedback arrow from Policy evaluation toward the Decision Model area; do not leave a floating dashed arrow or connect the feedback path to the Baselines table.
